#pragma once
#ifndef UTILS_HPP
#define UTILS_HPP

#define WITH_SCALE(value) (value * 4)

namespace Utils {
  float randomFloat(const float &min, const float &max);
} // namespace Utils

#endif // !UTILS_HPP
